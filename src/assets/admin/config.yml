backend:
  name: github
  repo: sajclarke/cms-sample

media_folder: media_folder

collections:
  - name: post
    label: Post
    folder: src/pages
    create: true
    fields:
      - {name: title, label: Title}
      - {name: body, label: Body, widget: markdown}